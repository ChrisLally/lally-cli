name: Release

on:
  push:
    tags:
      - "cli-v*"

permissions:
  contents: read
  id-token: write

jobs:
  publish-cli:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/release-publish.yml
    with:
      tag_prefix: "cli-v"
